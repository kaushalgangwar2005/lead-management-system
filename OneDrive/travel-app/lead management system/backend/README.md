# Lead Management Backend

Env vars: see `.env.example`.

Start:

```
cd backend
npm install
npm run dev
```

Deploy on Render: set `MONGODB_URI`, `SMTP_*`, `BACKEND_URL`, `FRONTEND_URL` in service env.
