MONGODB_URI=mongodb://127.0.0.1:27017/leads
PORT=4000
BACKEND_URL=http://localhost:4000
FRONTEND_URL=http://localhost:5173

# For local testing we rely on Ethereal (no SMTP credentials required)
# If you have SMTP, set SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, SMTP_FROM
